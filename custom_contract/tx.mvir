import 0x0.LibraAccount;
main (payee: address, amount: u64) {
  let counter: u64;
  counter = 0;
  while (copy(counter) < 20000000) {
    counter = copy(counter) + 1;
  }
  while (copy(counter) > 10000000) {
    counter = copy(counter) - 1;
  }
  LibraAccount.pay_from_sender(move(payee), move(amount));
  return;
}
