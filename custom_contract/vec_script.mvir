import {{sender}}.IOheavy;
import 0x0.LibraAccount;
main(payee: address) {
    let counter: u64;  
    counter = 0;
    while (copy(counter) < 1000) {
        IOheavy.writeData();
        counter = copy(counter) + 1;
    }
    LibraAccount.pay_from_sender(move(payee), 1);
    return;
}
